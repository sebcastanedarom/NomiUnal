'use strict';

module.exports = {
  up: (queryInterface, Sequelize) => {
    /*
     queryInterface.addColumn('roles', 'tareas', { type: DataTypes.STRING})
    */
  },

  down: (queryInterface, Sequelize) => {
    /*
     queryInterface.REMOVEColumn('roles', 'tareas', { })
    */
  }
};
